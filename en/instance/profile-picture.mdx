---
title: "Update profile picture"
api: "PUT /profile-picture"
description: "Change the profile picture of the WhatsApp connected to the instance"
---

## Overview

This method is responsible for changing your WhatsApp profile picture.

---

## Attributes

### Header

<ParamField header="Client-Token" type="string">
  Account security token
</ParamField>

### Body

<ParamField body="value" type="string" required>
  Image URL
</ParamField>

---

## Request Body

```json
{
  "value": "https://example.com/image.jpg"
}
```

---

## Response

### 200

<ResponseField name="value" type="boolean">
  `true` if successful, `false` if it failed
</ResponseField>

```json
{
  "value": true
}
```

### 405

Make sure you are correctly sending the method specification, that is, verify that you sent POST or PUT as specified at the beginning of this topic.

### 415

If you receive a 415 error, make sure to add the "Content-Type" header to your request, which in most cases is "application/json".
