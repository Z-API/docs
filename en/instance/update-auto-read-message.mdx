---
title: "Auto-read messages"
api: "PUT /instances/{instanceId}/token/{token}/update-auto-read-message"
description: "Enable or disable automatic reading of all messages received by the API"
---
import InstanceId from '/snippets/en/params/instance-id.mdx'
import Token from '/snippets/en/params/token.mdx'


## Overview

This method enables automatic reading of all messages received by the API.

---

## Attributes

### Header

<InstanceId />
<Token />
### Body

<ParamField body="value" type="boolean" required>
  `true` to enable auto-read or `false` to disable
</ParamField>

---

## Request Body

```json
{
  "value": true
}
```

---

## Response

### 200

```json
{
  "value": true
}
```

### 405

Make sure you are correctly sending the method specification, that is, verify that you sent POST or PUT as specified at the beginning of this topic.

### 415

If you receive a 415 error, make sure to add the "Content-Type" header to your request, which in most cases is "application/json".
