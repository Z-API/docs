---
title: "Delete queue message"
api: "DELETE /instances/{instanceId}/token/{token}/queue/{zaapId}"
description: "Delete a specific message from the queue"
---
import InstanceId from '/snippets/en/params/instance-id.mdx'
import Token from '/snippets/en/params/token.mdx'


## Overview

This method is responsible for deleting a specific message from the queue awaiting processing.

---

## Attributes

### Header

<InstanceId />
<Token />


<ParamField header="Client-Token" type="string">
  Account security token
</ParamField>

### Path

<ParamField path="zaapId" type="string" required>
  ID of the queue message to delete
</ParamField>

---

## Response

### 200

Message removed from queue successfully.

### 405

Make sure you are correctly sending the method specification, that is, verify that you sent DELETE as specified at the beginning of this topic.

### 415

If you receive a 415 error, make sure to add the "Content-Type" header to your request, which in most cases is "application/json".
