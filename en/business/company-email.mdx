---
title: "Update company email"
api: "POST /business/company-email"
description: "Updates the company email on WhatsApp Business"
---

## Overview

This method is responsible for updating the company email on the WhatsApp Business profile.

---

## Attributes

### Header

<ParamField header="Client-Token" type="string">
  Account security token
</ParamField>

### Required

<ParamField body="value" type="string" required>
  Company email. Send empty to remove
</ParamField>

---

## Request Body

```json
{
  "value": "email@example.com"
}
```

---

## Response

### 201

<ResponseField name="success" type="boolean">
  `true` if successful, `false` if it failed
</ResponseField>

```json
{
  "success": true
}
```

### 405

Make sure you are correctly sending the method specification, that is, verify that you sent POST or GET as specified at the beginning of this topic.

### 415

If you receive a 415 error, make sure to add the "Content-Type" header to your request, which in most cases is "application/json".
