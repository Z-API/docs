---
title: "Listar comunidades"
api: "GET /instances/{instanceId}/token/{token}/communities"
description: "Retorna todas as comunidades que você faz parte"
---
import InstanceId from '/snippets/pt/params/instance-id.mdx'
import Token from '/snippets/pt/params/token.mdx'


## Conceituação

Este método é responsável por retornar todas as comunidades que você faz parte.

---

## Atributos

### Header

<InstanceId />
<Token />
### Query Parameters

<ParamField query="page" type="integer">
  Utilizado para paginação, informe a página que quer buscar
</ParamField>

<ParamField query="pageSize" type="integer">
  Especifica o tamanho do retorno por página
</ParamField>

---

## Response

### 200

<ResponseField name="name" type="string">
  Nome da comunidade
</ResponseField>

<ResponseField name="id" type="string">
  ID da comunidade
</ResponseField>

```json
[
  {
    "name": "Minha primeira Comunidade",
    "id": "98372465382764532938"
  }
]
```

### 405

Neste caso certifique que esteja enviando corretamente a especificação do método, ou seja verifique se você enviou o POST ou GET conforme especificado no início deste tópico.

### 415

Caso você receba um erro 415, certifique de adicionar na headers da requisição o "Content-Type" do objeto que você está enviando, em sua grande maioria "application/json".
