---
title: "Recuperar código PIN"
api: "POST /instances/{instanceId}/token/{token}/mobile/recovery-pin-code"
description: "Solicita e-mail para recuperação do código PIN da verificação em duas etapas"
---
import InstanceId from '/snippets/pt/params/instance-id.mdx'
import Token from '/snippets/pt/params/token.mdx'


## Conceituação

Método utilizado para solicitar email para recuperação do código PIN da sua conta. Esse será útil caso você tenha configurado a verificação em duas etapas no WhatsApp e não se lembre mais desse código. Dessa forma, o WhatsApp enviará um link para redefinição de código PIN no email que você vinculou à sua conta do WhatsApp.

---

## Atributos

### Header

<InstanceId />
<Token />


<ParamField header="Client-Token" type="string">
  Token de segurança da conta
</ParamField>

---

## Response

### 200

<ResponseField name="success" type="boolean">
  Retorna true caso o e-mail de recuperação tenha sido enviado com sucesso
</ResponseField>

```json
{
  "success": true
}
```

### 400

Requisição inválida. Verifique se os dados que você está enviando estão de acordo com o documentado.

### 405

Neste caso certifique que esteja enviando corretamente a especificação do método, ou seja verifique se você enviou o POST ou GET conforme especificado no início deste tópico.

### 415

Caso você receba um erro 415, certifique de adicionar na headers da requisição o "Content-Type" do objeto que você está enviando, em sua grande maioria "application/json".
