---
title: "Listar categorias"
api: "GET /instances/{instanceId}/token/{token}/business/available-categories"
description: "Lista as categorias disponíveis para o WhatsApp Business"
---
import InstanceId from '/snippets/pt/params/instance-id.mdx'
import Token from '/snippets/pt/params/token.mdx'


## Conceituação

Este método é responsável por listar todas as categorias disponíveis para configuração no perfil do WhatsApp Business.

---

## Atributos

### Header

<InstanceId />
<Token />


<ParamField header="Client-Token" type="string">
  Token de segurança da conta
</ParamField>

### Query Parameters

<ParamField query="query" type="string">
  Busca por categoria
</ParamField>

---

## Response

### 200

<ResponseField name="displayName" type="string">
  Nome de exibição da categoria
</ResponseField>

<ResponseField name="label" type="string">
  Label da categoria
</ResponseField>

<ResponseField name="id" type="string">
  ID da categoria
</ResponseField>

```json
[
  {
    "displayName": "Outras empresas",
    "label": "OTHER_COMPANIES",
    "id": "629412378414563"
  }
]
```

### 405

Neste caso certifique que esteja enviando corretamente a especificação do método, ou seja verifique se você enviou o POST ou GET conforme especificado no início deste tópico.

### 415

Caso você receba um erro 415, certifique de adicionar na headers da requisição o "Content-Type" do objeto que você está enviando, em sua grande maioria "application/json".
